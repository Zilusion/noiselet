services:
  app-production:
    image: ghcr.io/zilusion/noiselet:latest
    container_name: noiselet-production
    restart: always
    ports:
      - '127.0.0.1:${PORT_PROD}:3000'
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL_PROD}
      - ORIGIN=${ORIGIN_PROD}
    volumes:
      - sqlite_data_prod:/app/.data

  app-edge:
    image: ghcr.io/zilusion/noiselet:edge
    container_name: noiselet-edge
    restart: always
    ports:
      - '127.0.0.1:${PORT_EDGE}:3000'
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATABASE_URL=${DATABASE_URL_EDGE}
      - ORIGIN=${ORIGIN_EDGE}
    volumes:
      - sqlite_data_edge:/app/.data

volumes:
  sqlite_data_prod:
  sqlite_data_edge:
